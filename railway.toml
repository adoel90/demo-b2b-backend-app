[build]
# Tidak perlu build ulang, cukup install dependency dari hasil `medusa build`
command = "cd backend/.medusa/server && npm install --omit=dev --force"


[deploy]
# Jalankan server yang sudah ter-bundle
start = "cd backend/.medusa/server && npm run predeploy && npm run start"